# Group depending on upstream's counterpart to make it easier to manage
# dependencies in hns/ (//chrome/utility does not depend on //hns/utility,
# so //hns/utility becomes essentially an alias for //chrome/utility now).
group("utility") {
  public_deps = [ "//chrome/utility" ]
}
